# Configuration for the sane-ricoh-spc240sf backend.
#
# List one printer per line: an IPv4 address or hostname. The backend
# will probe each one for eSCL first, then the Ricoh Web Image Monitor
# at port 80. Lines starting with '#' are ignored.
#
# Examples:
#   192.168.1.42
#   ricoh-mfp.lan
#
# Run `scanimage -L` after editing this file to verify discovery.
